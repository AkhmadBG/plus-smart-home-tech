server:
  port: 0

spring:
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:6541/commerce_shopping_store
    username: psht_db
    password: psht_db
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true